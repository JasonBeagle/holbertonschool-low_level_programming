#include "main.h"

/**
 *_abs - determines absolute value.
 *
 *@n:number to be computed.
 *
 * Return: the absolute value of the integer.
 *
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
