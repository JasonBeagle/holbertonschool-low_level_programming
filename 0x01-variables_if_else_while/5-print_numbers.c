#include <stdio.h>

/**
 *main - main will print numbers until 10 starting at 0
 *Return: always success 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
